package game.mechanics.game.gnu;

/**
 * This is the callback used for the GNU Game sequence. The GNU Game sequence is
 * essentially the 'story mode', and does not include the main menu, setting 
 * graphics options, alternative playmode options, etc,
 * 
 * Ideally, the GNU game is called when the user starts a new game, or enters a
 * saved one.
 * 
 * @author zkieda
 */
public class GNUGameCallback {
    //GNUCallback : 
    //  onGameFinish(EndState e)
            //make a profile or something. Not the menu screen.
    //  onGameStart(...)
        //FOR THE ENTIRE GAME, NOT ONE INSTANCE.
}