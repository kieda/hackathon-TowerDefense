package gui;

import tests.Paintable;

/**
 * @author zkieda
 */
public class Frame {
    public void addPaintable(Paintable p, int pos){}
    public void repaint(){
        
    }
    //getInputHandler(){
    //  setKey(kl) => frame.setKeyListener(kl)
    //  setMouseMotion(ml) => frame.setMouseMotionListener(ml)
    //  setMouse(ml) => frame.setMouseListener(ml)
    //  setWindow(wl) => frame.setWindowListener(wl)
    //}
    
}
